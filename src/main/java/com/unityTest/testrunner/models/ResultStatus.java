package com.unityTest.testrunner.models;

public enum ResultStatus {
    PASS,
    FAIL,
    COMPILATION_ERROR,
    RUNTIME_ERROR,
    OUT_OF_MEMORY_ERROR,
    TIMEOUT_ERROR,
    CONSTRAINT_VIOLATION_ERROR,
    UNEXPECTED_ERROR,
    IO_ERROR
}
